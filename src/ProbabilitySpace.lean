-- Let(X, Σ, P) be a probability space
-- where (X, Σ) is a measure space
-- and P: (X, Σ) → [0,1] is a probability measure

structure probability_space : Type := 
mk :: (a : ℕ )
#check probability_space
